import React from 'react';
import NavbarUserMenu from '../components/NavbarUserMenu/NavbarUserMenu';

// This is a wrapper component that Docusaurus will use
const CustomNavbarUserMenu: React.FC = () => {
  return <NavbarUserMenu />;
};

export default CustomNavbarUserMenu;